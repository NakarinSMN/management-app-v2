# --- เพิ่มเติมสำหรับโปรเจกต์ของคุณ ---

# 🔑 Secrets (สำคัญที่สุด!)
# กันไฟล์ .env ทุกรูปแบบ ทั้งที่อยู่ด้านนอกและในโฟลเดอร์ backend
.env
.env.*
backend/.env

# 📦 Dependencies
# ระบุเผื่อไว้สำหรับ node_modules ที่อยู่ในโฟลเดอร์ backend
backend/node_modules/

# 🗄️ Database
# ถ้า db.json คือข้อมูลลูกค้าจริงที่ไม่อยากให้หลุดไปบน GitHub ให้เอาคอมเมนต์ออกครับ
db.json

# --- ของเดิมที่คุณมี (ตรวจสอบความเรียบร้อย) ---
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?